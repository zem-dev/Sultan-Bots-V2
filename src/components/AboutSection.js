import "../css/about-section-styles.css"

const AboutSection = () => {
    return (
        <div className="about-section">
            <h1 className="about-section-title">Custom Bot for <span className="your-server-span">Your Server</span></h1>
            <p className="about-section-desc">Just tell us what you need and we’ll get it done. From utility to just fun commands, we can make bots equipped for any server.</p>
        </div>
    )
}

export default AboutSection;